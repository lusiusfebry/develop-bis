// Placeholder untuk seed data — akan diisi di Phase 2
async function main() {
    console.log('🌱 Seed placeholder — belum ada data untuk di-seed.');
}

main()
    .catch((e) => {
        console.error(e);
        process.exit(1);
    });
